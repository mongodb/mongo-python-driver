name: Python Tests

on:
  push:
    branches: ["main", "v**"]
  pull_request:
  workflow_dispatch:

concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash -eux {0}

permissions:
  contents: read

jobs:

  static:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false
      - name: Install Python tooling
        uses: mongodb-labs/drivers-github-tools/python/setup@1a2d86bd5fed4b0090453bcd510e4c2ba3b92356  # v3.0.4
        with:
          python-version: "3.10"
      # Runs before the install so a stale lock fails in seconds rather than
      # after the build and typecheck.
      - name: Check lockfile is up to date
        run: |
          uv lock --check
      - name: Install Python dependencies
        run: |
          just install
      - name: Run linters
        run: |
          just lint-manual
      - name: Run compilation
        run: |
          export PYMONGO_C_EXT_MUST_BUILD=1
          pip install -v -e .
          python tools/fail_if_no_c.py
      - name: Run typecheck
        run: |
          just typing
      - name: Run synchro smoke test
        run: |
          uv run --extra test --with unasync pytest tools/test_synchro.py -v --override-ini="addopts="
      - name: Validate pyproject.toml
        run: |
          uvx --from 'validate-pyproject[all]' validate-pyproject pyproject.toml
      - name: Check package metadata
        run: |
          uv build
          uvx twine check --strict dist/*
      - run: |
          sudo apt-get install -y cppcheck
      - run: |
          cppcheck --force bson
          cppcheck pymongo

  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # Tests currently only pass on ubuntu on GitHub Actions.
        os: [ubuntu-latest]
        python-version: ["3.10", "pypy-3.11", "3.13t", "3.15"]
        mongodb-version: ["8.0"]

    name: Python ${{ matrix.python-version }}-${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false
      - name: Install Python tooling
        uses: mongodb-labs/drivers-github-tools/python/setup@1a2d86bd5fed4b0090453bcd510e4c2ba3b92356  # v3.0.4
        with:
          # The beta Python here relies on the action's prerelease default.
          python-version: ${{ matrix.python-version }}
      - id: setup-mongodb
        uses: mongodb-labs/drivers-evergreen-tools@master
        with:
          version: "${{ matrix.mongodb-version }}"
      - name: Run tests
        run: uv run --extra test pytest -v

  coverage:
    # This enables a coverage report for a given PR, which will be augmented by
    # the combined codecov report uploaded in Evergreen.
    runs-on: ubuntu-latest

    name: Coverage
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false
      - name: Install Python tooling
        uses: mongodb-labs/drivers-github-tools/python/setup@1a2d86bd5fed4b0090453bcd510e4c2ba3b92356  # v3.0.4
        with:
          python-version: "3.10"
      - id: setup-mongodb
        uses: mongodb-labs/drivers-evergreen-tools@master
        with:
          version: "8.0"
      - name: Setup tests
        run: COVERAGE=1 just setup-tests
      - name: Run tests
        run: just run-tests
      - name: Generate xml report
        run: uv tool run --with "coverage[toml]" coverage xml
      - name: Upload test results to Codecov
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7.0.0
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
  doctest:
    runs-on: ubuntu-latest
    name: DocTest
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false
      - name: Install Python tooling
        uses: mongodb-labs/drivers-github-tools/python/setup@1a2d86bd5fed4b0090453bcd510e4c2ba3b92356  # v3.0.4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: just install
      - id: setup-mongodb
        uses: mongodb-labs/drivers-evergreen-tools@master
        with:
          version: "8.0"
      - name: Run tests
        run:  |
          just setup-tests doctest
          just run-tests

  mod_wsgi:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        mode: ["standalone", "embedded"]
    name: Mod WSGI (${{ matrix.mode }})
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false
      # endoflife.date returns releases newest-first, so the first entry is
      # the latest stable CPython.
      - id: latest-python
        run: echo "version=$(curl -fsSL https://endoflife.date/api/python.json | jq -r '.[0].latest')" >> "$GITHUB_OUTPUT"
      - name: Install Python tooling
        uses: mongodb-labs/drivers-github-tools/python/setup@1a2d86bd5fed4b0090453bcd510e4c2ba3b92356  # v3.0.4
        with:
          python-version: ${{ steps.latest-python.outputs.version }}
      - name: Install Apache
        run: sudo apt-get install -y apache2 apache2-dev
      - name: Install dependencies
        run: |
          export PYMONGO_C_EXT_MUST_BUILD=1
          just install
          # mod_wsgi >= 5 segfaults Apache children in embedded mode under load.
          uv pip install "mod_wsgi==4.9.4"
      - id: setup-mongodb
        uses: mongodb-labs/drivers-evergreen-tools@master
        with:
          version: "8.0"
          topology: "replica_set"
      - name: Run tests
        # Call the test scripts directly: the just recipes run "uv sync",
        # which would remove the ad-hoc mod_wsgi install before teardown can
        # stop Apache.
        run: |
          bash .evergreen/scripts/setup-tests.sh mod_wsgi ${{ matrix.mode }}
          bash .evergreen/run-tests.sh
      - name: Teardown
        if: always()
        run: bash .evergreen/scripts/teardown-tests.sh

  docs:
    name: Docs Checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false
      - name: Install Python tooling
        uses: mongodb-labs/drivers-github-tools/python/setup@1a2d86bd5fed4b0090453bcd510e4c2ba3b92356  # v3.0.4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: just install
      - name: Build docs
        run: just docs

  typing:
    name: Typing Tests
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python: ["3.10", "3.11"]
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false
      - name: Install Python tooling
        uses: mongodb-labs/drivers-github-tools/python/setup@1a2d86bd5fed4b0090453bcd510e4c2ba3b92356  # v3.0.4
        with:
          python-version: "${{matrix.python}}"
      - name: Install dependencies
        run: just install
      - name: Run typecheck
        run: |
          just typing

  integration_tests:
    runs-on: ubuntu-latest
    name: Integration Tests
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false
      - name: Install Python tooling
        uses: mongodb-labs/drivers-github-tools/python/setup@1a2d86bd5fed4b0090453bcd510e4c2ba3b92356  # v3.0.4
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: just install
      - id: setup-mongodb
        uses: mongodb-labs/drivers-evergreen-tools@master
      - name: Run tests
        run:  |
          just integration-tests
      - id: setup-mongodb-ssl
        uses: mongodb-labs/drivers-evergreen-tools@master
        with:
          ssl: true
      - name: Run tests
        run: |
          just integration-tests

  make_sdist:
    runs-on: ubuntu-latest
    name: "Make an sdist"
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-python@v7.0.0
        with:
          cache: 'pip'
          cache-dependency-path: 'pyproject.toml'
          # Build sdist on lowest supported Python
          python-version: "3.9"
      - name: Build SDist
        shell: bash
        run: |
          pip install build
          python -m build --sdist
      - uses: actions/upload-artifact@v7
        with:
          name: "sdist"
          path: dist/*.tar.gz

  test_sdist:
      runs-on: ubuntu-latest
      needs: [make_sdist]
      name: Install from SDist and Test
      timeout-minutes: 20
      steps:
        - name: Download sdist
          uses: actions/download-artifact@v8
          with:
            path: sdist/
        - name: Unpack SDist
          shell: bash
          run: |
            cd sdist
            ls
            mkdir test
            tar --strip-components=1 -zxf *.tar.gz -C ./test
            ls test
        - uses: actions/setup-python@v7.0.0
          with:
            cache: 'pip'
            cache-dependency-path: 'sdist/test/pyproject.toml'
            # Test sdist on lowest supported Python
            python-version: "3.9"
        - id: setup-mongodb
          uses: mongodb-labs/drivers-evergreen-tools@master
        - name: Run connect test from sdist
          shell: bash
          run: |
            cd sdist/test
            ls
            which python
            pip install -e ".[test]"
            PYMONGO_MUST_CONNECT=1 pytest -v -k client_context

  test_minimum:
      permissions:
        contents: read
      runs-on: ubuntu-latest
      name: Test minimum dependencies and Python
      steps:
        - uses: actions/checkout@v7.0.1
          with:
            persist-credentials: false
        - name: Install Python tooling
          uses: mongodb-labs/drivers-github-tools/python/setup@1a2d86bd5fed4b0090453bcd510e4c2ba3b92356  # v3.0.4
          with:
            python-version: "3.9"
        - id: setup-mongodb
          uses: mongodb-labs/drivers-evergreen-tools@master
          with:
            version: "8.0"
        - name: Run tests
          shell: bash
          run: |
            uv venv
            source .venv/bin/activate
            uv pip install -e ".[test]" --resolution=lowest-direct --force-reinstall
            pytest -v test/test_srv_polling.py test/test_dns.py test/asynchronous/test_srv_polling.py test/asynchronous/test_dns.py

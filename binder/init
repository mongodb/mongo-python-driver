VERSION=`cat VERSION`
PORT=`cat PORT`
MONGODB_BIN=`m bin $VERSION`

pkill mongod
rm -rf data

mlaunch init --binarypath \
$MONGODB_BIN --replicaset --name repl0 --nodes 3 --port $PORT --hostname localhost --setParameter --priority \
enableTestCommands=1
